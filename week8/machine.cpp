#include<bits/stdc++.h>
using namespace std;
typedef struct time_t {
    int start;
    int terminate;
} time;
int main() {
    int n;
    cin >> n;
    time a[n];
    for(int i = 0; i < n; i++) {
        cin >> a[i].start >> a[i].terminate;
    }

}