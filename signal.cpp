using namespace std;
#include<iostream>
#include<algorithm>
int max(int a, int b) {
    if(a < b) return b;
    else return a;
}
int main() {
    int n, b;
    int a[n];
    scanf("%d %d", &n, &b);
    for(int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    
}
